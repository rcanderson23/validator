apiVersion: v1
kind: Service
metadata:
  labels:
    run: validator
  name: validator
  namespace: kube-system
spec:
  ports:
  - port: 8443
    protocol: TCP
    targetPort: 8443
  selector:
    run: validator
  sessionAffinity: None
  type: ClusterIP
